# API Configuration
# Development: Use localhost for MongoDB
# Production: Use mongodb service name for Docker networking
NODE_ENV=development
PORT=3000
MONGODB_URI=mongodb://localhost:27017/easygenerator
JWT_SECRET=your-secret-key-change-in-production

# MongoDB Configuration (used in docker-compose)
MONGO_INITDB_DATABASE=easygenerator
