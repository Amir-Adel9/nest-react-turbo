# Copy to .env and adjust. Used by docker-compose (prod + dev).

# Gateway host port for prod (default 8080). Use 80 for http://localhost when port 80 is free.
GATEWAY_PORT=8080

# MongoDB database name (dev + prod)
MONGO_INITDB_DATABASE=easygenerator
